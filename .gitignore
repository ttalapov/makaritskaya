# dependencies
node_modules/

# build output (appears at Step 3)
dist/

# editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# scratch
*.log
_*.html

# camera originals: needed to regenerate the derivatives, never deployed,
# never committed. build.mjs copies assets/ wholesale, so sources live
# outside it by construction rather than by a filter someone can forget.
sources/

# fonts fetched by tools/og-card.py on first run
tools/.fonts/
